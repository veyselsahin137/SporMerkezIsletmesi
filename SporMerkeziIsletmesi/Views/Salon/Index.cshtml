@model IEnumerable<SporMerkeziIsletmesi.Models.Salon>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SalonAdi)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Adres)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AcilisSaati)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.KapanisSaati)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SalonAdi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Adres)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AcilisSaati)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.KapanisSaati)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SalonID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SalonID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SalonID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
